name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - dev
    paths-ignore:
      - '.idea/**'
      - '.gitignore'
      - '*.md'
      - 'LICENSE'
  pull_request:
    paths-ignore:
      - '.idea/**'
      - '.gitignore'
      - '*.md'
      - 'LICENSE'

jobs:
  # build:
  #   runs-on: macos-latest
  #   env:
  #     JAVA_VERSION: 17
  #     JAVA_DISTRO: 'temurin'
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       module: [sample]
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Validate Gradle Wrapper
  #       uses: gradle/actions/wrapper-validation@v3

  #     - name: Set up JDK 17
  #       uses: actions/setup-java@v4
  #       with:
  #         java-version: ${{ env.JAVA_VERSION }}
  #         distribution: ${{ env.JAVA_DISTRO }}

  #     - name: Set up gradle
  #       uses: gradle/actions/setup-gradle@v4

  #     - name: Build ${{ matrix.module }} Package
  #       run: |
  #         ./gradlew :${{ matrix.module }}:assembleRelease --parallel

  #     - name: Upload APK ${{ matrix.module }}
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: apk-${{ matrix.module }}-${{ github.run_number }}
  #         path: ${{ matrix.module }}/build/outputs/apk/release/sample-release.apk
  
  build-kotlin:
    runs-on: macos-latest
    env:
      JAVA_VERSION: 17
      JAVA_DISTRO: 'temurin'
    steps:
      - uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRO }}

      - name: Set up gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build kotlin Package
        run: |
          ./gradlew :tesseract4android-ktx:assembleRelease --parallel --configuration-cache